Сделано с целью отрабатки навык применения паттерна Наблюдатель.
Вообще это должен быть транслятор из JavaScript кода в C++ код.
Но в данном случае интересуеn именно реализация паттерна Observer, поэтому "интерпретатор" 
всегда находит в коде одну целочисленную переменную ).
