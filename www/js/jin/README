Сделано с целью отработки навыка применения паттерна Наблюдатель.
Вообще это должен быть транслятор из JavaScript кода в C++ код.
Но в данном случае интересует именно реализация паттерна Observer, поэтому "интерпретатор" 
всегда находит в коде одну целочисленную переменную ).

